{
  "name": "triplep-tools",
  "private": true,
  "description": "Test suite for the TripleP Tools pages (the site itself needs no build step).",
  "scripts": {
    "test": "node tests/run.js"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
